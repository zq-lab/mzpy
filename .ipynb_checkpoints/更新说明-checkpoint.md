# 更新说明
version 0.7.0

- peakFrame增加了函数：convert_ms_array
- 增加了子包mzX，用于提供mzpy和其它质谱工具包之间的调用连接层
    - mzX中，有些包的需求比较特殊，因此，其中的每个脚本在加载时临时检查依赖包是否存在
    - 这样做的好处时，在不具备这些特殊包依赖时，mzX外层的其它脚本不受影响，不强制必须满足依赖
- 降低依赖包的要求，以适配dreaMS的运行环境
    - 特别是numba、numpy和pandas三个包
- 